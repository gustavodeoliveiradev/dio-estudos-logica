const mensagem = "Bem-vindo ao mundo dos jogos!";

// saída
console.log(mensagem + " Prepare-se para uma aventura incrível!");

let notificacao = "Pokemon Go diz: ";

// saída
console.log(notificacao +"tem um novo pokemon na região");
console.log(notificacao + "você foi derrotado por um líder");

notificacao = "Digimon Go"

console.log("o nome do jogo agora é: " + notificacao);

// variáveis podem ser reatribuídas, ou seja, podem receber um novo valor
// como vimos anteriormente, uma variável let pode ser reatribuída, ou seja, pode receber um novo valor.
// já uma variável const(constante) não pode ser reatribuída, ou seja, não pode receber um novo valor.

/* Quando usamos uma constante:
Quando temos uma declaração de mensagem, número ou tipo que NÃO queremos alterar 

Quando usamos uma variável let:
Quando temos uma declaração de mensagem, número ou tipo que pode ser alterado */